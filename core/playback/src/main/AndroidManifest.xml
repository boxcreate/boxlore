<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Match :core:data defaults; :app overrides via tools:replace where needed. -->
    <application
        android:allowBackup="false"
        android:usesCleartextTraffic="false">
        <!-- FQCN must stay cx.aswin.boxlore.core.data.service.* (package unchanged). -->
        <service
            android:name="cx.aswin.boxlore.core.data.service.MediaDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="androidx.media3.exoplayer.downloadService.action.RESTART" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>
    </application>

</manifest>
